import 'package:flutter/material.dart';

class AppShadows {
  static final BoxShadow soft = BoxShadow(
    color: const Color.fromRGBO(15, 23, 42, 0.02),
    blurRadius: 24.0,
    offset: const Offset(0, 8),
  );
}
